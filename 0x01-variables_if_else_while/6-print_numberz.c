#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int mub = '0';

	for (num = '0'; num <= '9'; mum++)
		putchar(num);

	putchar('\n');
	return (0);
}
