#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int mub = '0';

	for (mub = '0'; mub <= '9'; mub++)
		putchar(mub);

	putchar('\n');
	return (0);
}
