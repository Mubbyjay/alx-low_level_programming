#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mub = 'z';

	for (mub = 'z'; mub >= 'a'; mub--)
		putchar(mub);

	putchar('\n')

	return (0);
}
